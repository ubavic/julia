#ifndef ESCAPE_TIME_H
#define ESCAPE_TIME_H

#include "global.h"

int DrawEscapeTime ();

complex Mandelbrot (complex, complex);

complex Multibrot (complex, complex);

complex Mandelbar (complex, complex);

complex Ship (complex, complex);

#endif
